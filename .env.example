# ===========================================
# Desenvolvimento local (sem Docker Compose)
# ===========================================
#
# 1. PostgreSQL: precisa estar rodando localmente.
#    Ex: docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=bolao postgres:16-alpine
#
# 2. API: cd api && go run ./cmd/server
#    Variáveis (opcional, já tem defaults):
#    export DATABASE_URL=postgres://postgres:postgres@localhost:5432/bolao?sslmode=disable
#    export PORT=8080
#
# 3. Web: cd web && npm run dev
#    Se a API estiver em outra porta, crie web/.env com:
#    VITE_API_URL=http://localhost:3333
